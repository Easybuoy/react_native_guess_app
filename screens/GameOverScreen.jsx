import React from "react";
import { View, Text, StyleSheet } from "react-native";
const GameOver = () => {
  return (
    <View>
      <Text>Game Oiver</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default GameOver;
